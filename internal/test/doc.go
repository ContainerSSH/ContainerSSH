// Package test provides helpers for launching services for testing. Most services run in Docker containers and expect
// Docker to be available in its standard place via a Docker socket.

package test
